start: install dev

install:
	npm install

dev:
	npm run dev

build:
	npm run build

build-sb:
	npm run build:prod

clean:
	npm run clean

format:
	npm run format

format-check:
	npm run format:check

test:
	npm run test
